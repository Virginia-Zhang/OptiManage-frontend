module.exports = {
    // You can ignore the parentheses when the arrow function has only one parameter.
    arrowParens: 'avoid',
    // No spaces should appear inside the brackets
    bracketSpacing: true,
    // Line terminators use Unix format
    endOfLine: 'lf',
    // true: Put > on the last line instead of at a new line
    jsxBracketSameLine: false,
    // line width
    printWidth: 100,
    // Line break
    proseWrap: 'preserve',
    // semicolon
    semi: false,
    // Use single quotes
    // singleQuote: true,
    // indentation
    tabWidth: 4,
    // Use tab indentation
    useTabs: true,
    // Post comma. For multi-line objects and arrays, add a comma in the last line.
    trailingComma: 'es5',
  }